<section class="sub-nav">
    <section class="sub-nav-left">
        <h2><%= season %> <%= year %></h2>
    </section>
</section>

<section class="season-container">
    <% anime.forEach(anime => { %>
        <section class="season-entry">
        <img src="<%= anime.image_url %>" alt="<%= anime.title %> art.">
        <p class="season-title"><%= anime.title %></p>
        <p><%= anime.synopsis.slice(0, 144) %>... <a href="/anime/<%= anime.mal_id %>">Read More</a></p>
        </section>
    <% }) %>
</section>